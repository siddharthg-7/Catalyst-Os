TASK DELEGATED BY CEO: "{{task}}"

STARTUP STATE:
{{context}}

KNOWLEDGE DISCLOSURES (RAG):
{{rag}}

Formulate system architecture, server deployment speedups, tool setup, and SOC-2 security protocols. Return a JSON matching the requested schema.
